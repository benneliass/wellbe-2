import { OnboardingFlow } from "@/components/auth/OnboardingFlow";

/** First-run onboarding: welcome, core consent, minimal baseline, confirm. */
export default function OnboardingPage() {
  return <OnboardingFlow />;
}

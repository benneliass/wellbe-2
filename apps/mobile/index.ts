// Placeholder entry so the @wellbe/mobile workspace type-checks cleanly until the
// Expo app is built out. Replace with the real app entry (App.tsx) when work starts.
export const MOBILE_APP_NAME = "WellBe";

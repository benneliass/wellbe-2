apiVersion: v1
kind: Service
metadata:
  name: vault-writer
  labels:
    {{- include "wellbe-local.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: vault-writer
  ports:
    - port: {{ .Values.vaultWriter.port }}
      targetPort: {{ .Values.vaultWriter.port }}

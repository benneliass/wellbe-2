apiVersion: v1
kind: Service
metadata:
  name: ingestion-worker
  labels:
    {{- include "wellbe-local.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: ingestion-worker
  ports:
    - port: {{ .Values.ingestionWorker.port }}
      targetPort: {{ .Values.ingestionWorker.port }}

apiVersion: v1
kind: Service
metadata:
  name: zitadel
  labels:
    {{- include "wellbe-local.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: zitadel
  ports:
    - port: {{ .Values.zitadel.externalPort }}
      targetPort: 8080

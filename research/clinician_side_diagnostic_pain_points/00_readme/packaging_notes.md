# Packaging notes

Files were organized according to the requested directory structure. Structured data was written as CSV. Readable notes and summaries were written as Markdown.

Cleanup performed:

- Converted the prior research report into source, evidence, fact, quote, case, and metric tables.
- Added `research_topic` and `date_packaged` to structured rows.
- Added `.keep` files in empty directories.
- Created short source-summary files where raw source files were not available.

Assumptions:

- `date_packaged` is 2026-05-30.
- The topic slug is `clinician_side_diagnostic_pain_points`.
- Evidence-quality labels follow the prior research pack's definitions.

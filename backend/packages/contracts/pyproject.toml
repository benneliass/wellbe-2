[project]
name = "wellbe-contracts"
version = "0.1.0"
requires-python = ">=3.13"
description = "Shared Pydantic DTOs and event types. No component implementations may be imported here."
dependencies = [
    "pydantic>=2.10",
]

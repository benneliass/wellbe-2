[project]
name = "wellbe-c3-ingestion"
version = "0.1.0"
requires-python = ">=3.13"
description = "Ingestion Layer (C3): source-type adapters writing into the Vault."
dependencies = [
    "wellbe-contracts",
    "wellbe-platform",
    "httpx>=0.28",
]

[tool.uv.sources]
wellbe-contracts = { workspace = true }
wellbe-platform = { workspace = true }
